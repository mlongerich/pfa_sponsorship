<h1>New Sponsorship Type</h1>
<%= form_for(@sponsorship_type) do |f| %>
  <% if @sponsorship_type.errors.any? %>
    <div id="errors explanation"> 
      <h2><%= pluralize(@sponsorship_type.errors.count, "error") %> prohibited this sponsorship type from being saved:</h2>
      <ul>
        <% @sponsorship_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  <%= f.label :kind %> <br>
  <%= f.text_field :kind %>
</div>

<div class="field">
  <%= f.label :amount %> <br>
  <%= f.number_field :amount %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
<% end %>

<%= link_to 'Back', sponsorship_types_path %>
