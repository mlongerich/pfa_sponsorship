<%= form_for(@payment) do |f| %>
  <% if @payment.errors.any? %>
    <div id="errors explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>
      <ul>
        <% @payment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  <%= f.label :month_year %> <br>
  <%= f.date_select :month_year %>
</div>

<div class="field">
  <%= f.label :sponsorship_id %> <br>
  <%= f.number_field :sponsorship_id %>
</div>

<div class="field">
  <%= f.label :method %> <br>
  <%= f.text_field :method %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
<% end %>
